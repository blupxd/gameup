import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['ddragon.leagueoflegends.com','avatars.steamstatic.com', 'media.valorant-api.com','avatars.fastly.steamstatic.com'],
  }
};

export default nextConfig;
